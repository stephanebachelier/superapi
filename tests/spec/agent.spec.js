define([
  "superapi"
], function (superapi) { // jshint ignore:line
  "use strict";

  describe.skip("agent", function () {
    it("should support setting agent from constructor", function () {

    });

    it("should support configuring agent on api instance", function () {

    });
  });
});
